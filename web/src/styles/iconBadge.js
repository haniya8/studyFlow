export const iconBadgeStyle = (bg = 'primary.main', size = 40) => ({
  width: size,
  height: size,
  borderRadius: '10px',
  bgcolor: bg,
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center',
  flexShrink: 0,
});